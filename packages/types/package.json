{
  "name": "@tradeinsight/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for TradeInsight",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test:coverage": "echo 'No tests in types package'",
    "test": "echo 'No tests in types package'"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}